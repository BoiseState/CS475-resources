apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-app-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "my-app-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-app-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
